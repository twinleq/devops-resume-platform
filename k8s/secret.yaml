apiVersion: v1
kind: Secret
metadata:
  name: resume-app-secrets
  namespace: devops-resume
  labels:
    app.kubernetes.io/name: devops-resume-platform
    app.kubernetes.io/part-of: devops-resume-platform
type: Opaque
data:
  # Base64 encoded values (example values)
  # Replace with actual base64 encoded secrets
  # To encode: echo -n "your-secret" | base64
  database_url: ""
  api_key: ""
  jwt_secret: ""
stringData:
  # These will be automatically base64 encoded
  # Use stringData for easier management
  environment: "production"
  debug: "false"

